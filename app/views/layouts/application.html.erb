<!DOCTYPE html>
<html>
<head>
  <title>SecureInventory</title>
  <link href='https://fonts.googleapis.com/css?family=Open+Sans:400,300,700,800,600' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="background <%= current_page?(root_path) || current_page?(new_user_path) || current_page?(about_path) || current_page?(login_path) ? "white" : "black" %>">
    <header>
      <div>
        <a class='logo' href= "/" <% root_path %> >secure<span id='logo_thin'>inventory</span></a>
      </div>
      <nav>
        <ul>
          <% if current_user %>
            <li class=" <%= 'active' if current_page?(items_path)%>"><%= link_to 'Your Inventory', items_path %></li>
            <li class=" <%= 'active' if current_page?(user_path(current_user))%>"><%= link_to 'Your Account', user_path(current_user) %></li>
            <li><%= link_to 'Sign Out', logout_path, method: :delete %></li>
          <% else %>
            <li class=" <%= 'active' if current_page?(about_path)%>" ><%= link_to 'About Us', about_path %></li>
            <li><%= link_to 'Create Account', new_user_path %></li>
            <li><%= link_to 'Sign In', login_path %>
          <% end %>
        </ul>
      </nav>
    </header>

    <%= yield %>

  </div>
</body>
</html>
